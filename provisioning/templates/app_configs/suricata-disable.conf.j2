# =======================================================================
# SURICATA TUNING: COMMON HOME / IOT / OFFLOADING FALSE POSITIVES
# =======================================================================
# suricata-update reads this file and strips matching SIDs before
# writing the final ruleset, so they never fire at runtime.

# --- 1. TCP/Stream Engine Anomalies (Caused by cheap IoT NICs & VM Offloading) ---
# When Linux GRO/LRO clumps packets together, or IoT devices hard-close connections,
# the Suricata stream-tracking engine gets pedantic and throws these protocol errors.
2260001  # SURICATA Applayer Wrong direction first Data
2210042  # SURICATA STREAM TIMEWAIT ACK with wrong seq
2210044  # SURICATA STREAM Packet with invalid timestamp
2210045  # SURICATA STREAM Packet with invalid ack
2210020  # SURICATA STREAM ESTABLISHED packet out of window
2210029  # SURICATA STREAM ESTABLISHED RST out of window
2210008  # SURICATA STREAM 3way handshake wrong seq wrong ack (Mobile devices roaming)
2210010  # SURICATA STREAM 3way handshake SYNACK with wrong ack
2210030  # SURICATA STREAM FIN invalid ack: common in noisy network environments; typically a transient state mismatch rather than malicious activity
2210038  # SURICATA STREAM FIN out of window: common in high-traffic environments and does not represent a security threat
2210046  # SURICATA STREAM SHUTDOWN RST invalid ack: common TCP stream engine alert in noisy network environments

# --- 2. Smart Home / IoT Broadcast Chatter ---
# Smart TVs, printers, and media players constantly yell their presence
# to the local network using UPnP and SSDP. These are safe on an internal VLAN.
2013028  # ET POLICY UPnP SUBSCRIBE method
2013031  # ET POLICY UPnP NOTIFY method
2224005  # SURICATA DNS malformed request data (mDNS / Bonjour traffic often triggers this)
2230009  # SURICATA HTTP response has no URI (Often triggered by weird SSDP responses)

# --- 3. Mobile Device / VoIP Traffic (STUN / WebRTC) ---
# WhatsApp, FaceTime, Discord, and WebRTC use STUN to punch holes through NAT.
# EmergingThreats flags these as "INFO", but mobile phones spam them constantly.
# The "non-standard high port" variant fires per-packet during ICE negotiation,
# generating 60+ alerts per failed P2P candidate before the client falls back to relay.
2011507  # ET INFO Session Traversal Utilities for NAT (STUN Binding Request)
2016149  # ET INFO Session Traversal Utilities for NAT (STUN Binding Response)
2033078  # ET INFO STUN Binding Request On Non-Standard High Port: routine STUN traffic commonly associated with WebRTC and VoIP applications; expected behavior in modern network environments and does not indicate a security threat
2036220  # ET INFO Android Device Connectivity Check: standard Android connectivity checks are routine background traffic and do not indicate malicious activity or security incidents

# --- 4. Cloud / Protocol Diagnostic Noise ---
# Report: Monthly Suricata AI Triage (2026-03-03 through 2026-03-31, end exclusive), run_id=manual__2026-04-23T14:25:42.202385+00:00
2050736  # ET INFO Tencent Cloud Storage Domain in DNS Lookup: routine DNS lookups for Tencent Cloud storage domains are common infrastructure traffic and do not indicate malicious activity in this context
2052580  # ET INFO DNS Query to Alibaba Cloud CDN Domain: routine DNS queries to Alibaba Cloud CDN are common infrastructure traffic and do not indicate malicious activity in this context
2052581  # ET INFO Observed Alibaba Cloud CDN Domain in TLS SNI: routine traffic to Alibaba Cloud CDN is common infrastructure and does not indicate malicious activity in this context
2260002  # SURICATA Applayer Detect protocol only one direction: parser only observed traffic in one direction, which is common in incomplete sessions or specific protocol handshakes and does not represent a security threat
2260003  # SURICATA Applayer Protocol detection skipped: application layer protocol detection was skipped, which is a common informational message in Suricata when traffic does not match known protocol patterns or is malformed

# --- 5. TCP Session Noise from Broken Links / Lossy Paths ---
# These stream-engine alerts are common on asymmetric, lossy, or heavily NATed paths.
2210054  # SURICATA STREAM excessive retransmissions: network congestion or packet loss is common in standard network operations and does not represent a specific security threat requiring individual investigation

# --- 6. Harmless TLS / Crypto Warnings ---
# Modern browsers and apps frequently negotiate TLS closures abruptly, or use
# newer QUIC protocols that Suricata sometimes misclassifies.
2221010  # SURICATA TLS error message warning
2221029  # SURICATA TLS invalid record type

# --- 7. Legacy Auth / Stream Diagnostic Noise ---
2067086  # ET INFO NTLMv1 Session Setup Response - Challenge: NTLMv1 is an outdated authentication protocol often flagged in enterprise environments due to legacy compatibility requirements. A single occurrence without further context is typical background noise.
# run_id=scheduled__2026-05-01T03:00:00+00:00 | Signature ET INFO NTLMv1 Session Setup Response - Challenge was classified as background noise for the full reporting window.
threshold.config: suppress gen_id 1, sig_id 2067086
2260003  # SURICATA Applayer Protocol detection skipped: This alert indicates that the protocol detection engine skipped analysis for a specific flow, which is a common diagnostic message in Suricata and does not represent a security threat or actionable event.
# run_id=scheduled__2026-05-01T03:00:00+00:00 | Signature SURICATA Applayer Protocol detection skipped was classified as background noise for the full reporting window.
threshold.config: suppress gen_id 1, sig_id 2260003
2210016  # SURICATA STREAM CLOSEWAIT FIN out of window: This alert indicates a TCP stream state mismatch, which is common in network environments with asymmetric routing or packet loss and does not represent a security threat.
# run_id=scheduled__2026-05-01T03:00:00+00:00 | Signature SURICATA STREAM CLOSEWAIT FIN out of window was classified as background noise for the full reporting window.
threshold.config: suppress gen_id 1, sig_id 2210016

# --- 8. Scheduled Background Noise Suppressions ---
2057745  # ET INFO DNS Query to Cloudflare Page Developer Domain (pages .dev): The signature tracks DNS queries to a common cloud hosting platform (Cloudflare Pages), which is standard web traffic and does not indicate malicious intent in this context.
# run_id=scheduled__2026-06-01T03:00:00+00:00 | Signature ET INFO DNS Query to Cloudflare Page Developer Domain (pages .dev) was classified as background noise for the full reporting window.
threshold.config: suppress gen_id 1, sig_id 2057745
2051767  # ET INFO Observed DNS Query to Cloudflare Developer Domain (workers .dev): The alert tracks DNS queries to Cloudflare's workers.dev domain, which is a common platform for legitimate web applications and services. Given the high volume and benign nature of this traffic in modern network environments, it constitutes routine background noise.
# run_id=scheduled__2026-06-01T03:00:00+00:00 | Signature ET INFO Observed DNS Query to Cloudflare Developer Domain (workers .dev) was classified as background noise for the full reporting window.
threshold.config: suppress gen_id 1, sig_id 2051767
2016150  # ET INFO Session Traversal Utilities for NAT (STUN Binding Response): The signature represents routine STUN protocol traffic, which is standard for NAT traversal in VoIP and WebRTC applications. The high volume of alerts is consistent with expected background noise for this protocol.
# run_id=scheduled__2026-06-01T03:00:00+00:00 | Signature ET INFO Session Traversal Utilities for NAT (STUN Binding Response) was classified as background noise for the full reporting window.
threshold.config: suppress gen_id 1, sig_id 2016150
2210027  # SURICATA STREAM ESTABLISHED SYN resend with different seq: This signature is a common Suricata engine alert indicating a TCP stream anomaly often caused by network jitter or retransmissions in established connections, which is typically considered routine background noise.
# run_id=scheduled__2026-06-01T03:00:00+00:00 | Signature SURICATA STREAM ESTABLISHED SYN resend with different seq was classified as background noise for the full reporting window.
threshold.config: suppress gen_id 1, sig_id 2210027
2210023  # SURICATA STREAM ESTABLISHED SYNACK resend with different ACK: This is a standard Suricata stream engine alert indicating a TCP retransmission anomaly, which is common in noisy network environments and does not typically represent a security threat.
# run_id=scheduled__2026-06-01T03:00:00+00:00 | Signature SURICATA STREAM ESTABLISHED SYNACK resend with different ACK was classified as background noise for the full reporting window.
threshold.config: suppress gen_id 1, sig_id 2210023
2200078  # SURICATA UDPv6 invalid checksum: This is a single instance of a checksum validation error, which is common in network traffic due to transient transmission issues or malformed packets and does not indicate a targeted security threat.
# run_id=scheduled__2026-06-01T03:00:00+00:00 | Signature SURICATA UDPv6 invalid checksum was classified as background noise for the full reporting window.
threshold.config: suppress gen_id 1, sig_id 2200078
