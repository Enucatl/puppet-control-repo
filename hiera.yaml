---
version: 5

defaults:
  datadir: "data"
  data_hash: yaml_data

hierarchy:
  - name: "Single node"
    path: "nodes/%{trusted.certname}.yaml"

  - name: "OS defaults"
    path: "os/%{facts.os.family}.yaml"

  - name: "Common data for all nodes"
    path: "common.yaml"
