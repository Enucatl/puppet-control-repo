[project]
name = "puppet-control-repo-tests"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "niquests>=3.18.8",
    "transitions",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
]

[tool.uv]
package = false
